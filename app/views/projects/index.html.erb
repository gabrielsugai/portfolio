<% if @projects.any? %>
  <% @projects.each do |project| %>
    <p>Projeto: <%= project.name %></p>
    <p>Descrição: <%= project.description %></p>
    <p>Repositório: <%= project.repository %></p>
  <% end %>
<% else %>
  <p>Nenhum projeto cadastrado.</p>
<% end %>